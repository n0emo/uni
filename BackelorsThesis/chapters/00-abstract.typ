В настоящей выпускной квалификационной работе разработана встраиваемая среда выполнения
WASM-компонентов Wassel, реализующая интерфейс взаимодействия между информационной системой и
пользовательскими программами на основе стандарта WASI~0.2 и компонентной модели WASM.

В ходе работы проведён анализ существующих архитектур развёртывания сервисов и платформ
Function-as-a-Service, обоснован выбор технологического стека, спроектированы архитектура системы и
WIT-интерфейсы взаимодействия. Разработанная система включает высокопроизводительный HTTP-сервер,
изолированное исполнение WASM-компонентов, интерфейсы для работы с PostgreSQL и Redis, SDK для Rust
и Python, интерфейс командной строки и административную панель мониторинга. Выполнено
технико-экономическое обоснование разработки и анализ условий труда разработчика.

Пояснительная записка содержит #{
  let pages = context { counter(page).final().at(0) }
  pages
} страниц, #{
  let figures = context { counter(figure.where(kind: image)).final().at(0) }
  figures
} рисунков и #{
  let tables = context { counter(figure.where(kind: table)).final().at(0) }
  tables
} таблицы.
